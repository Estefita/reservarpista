<form method="post" action="{{ path('pista_delete', {'id': pistum.id}) }}" onsubmit="return confirm('¿Desea eliminar?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pistum.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
