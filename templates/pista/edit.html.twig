{% extends 'base.html.twig' %}

{% block title %}Editar Pista{% endblock %}

{% block body %}
	{% include "section/section.html.twig" with {'descripcion':'Pista'} %}
	<div class="container-fluid p-5">
		<h3 class="text-center">Editar Pista</h3>
		<div class="row justify-content-center">
			<div class="col-4">
				<div class="classes__item__text">
					{{ include('pista/_form.html.twig', {'button_label': 'Actualizar'}) }}
					<a href="{{ path('pista_index') }}" class="class-btn mt-2">Volver al listado</a>
					{# {{ include('pista/_delete_form.html.twig') }} #}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
