{% extends 'base.html.twig' %}

{% block title %}Nueva Pista Pista{% endblock %}

{% block body %}
{# {% include "section/section.html.twig" with {'descripcion':'Pista'} %}
    <h1>Crear Nueva Pista</h1>

    {{ include('pista/_form.html.twig') }}

    <a href="{{ path('pista_index') }}">back to list</a> #}

 {% include "section/section.html.twig" with {'descripcion':'Pista'} %}
	<div class="container-fluid p-5">
		<h3 class="text-center">Crear Nueva Pista</h3>
		<div class="row justify-content-center">
			<div class="col-4">
				{{ include('pista/_form.html.twig') }}
				<div class="classes__item__text pl-0 text-center">
					<a href="{{ path('pista_index') }}" class="class-btn">Volver al listado</a>
					
				</div>
			</div>
		</div>
	</div>

{% endblock %}
