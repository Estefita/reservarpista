{% extends 'base.html.twig' %}

{% block title %}Nuevo Tipo{% endblock %}

{% block body %}
{% include "section/section.html.twig" with {'descripcion':'Tipo Pista'} %}

    <div class="container-fluid p-5">
    <h3 class="text-center">Crear Nuevo Tipo</h3>
        <div class="row justify-content-center">
            <div class="col-4">
                {{ include('tipo/_form.html.twig') }}
                <div class="classes__item__text pl-0 text-center">  
                    <a href="{{ path('tipo_index') }}" class="class-btn">Volver al listado</a>
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
