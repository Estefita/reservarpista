{% extends 'base.html.twig' %}

{% block title %}Tipo{% endblock %}

{% block body %}
{% include "section/section.html.twig" with {'descripcion':'Tipo Pista'} %}

    <div class="container-fluid p-5">
    <h1 class="text-center">Tipo</h1>
        <div class="row justify-content-center">
            
            <div class="col-lg-4 col-md-6">
                <div class="classes__item classes__item__page">
                    <div class="classes__item__pic set-bg" data-setbg="img/classes/classes-3.jpg">
                        <span>Foto</span>
                    </div>                            
                    <h6>Nombre: {{ tipo.nombre }}</h6>
                    <h6>Fecha creación: {{ tipo.fechacreacion ? tipo.fechacreacion|date('Y-m-d H:i:s') : '' }}</h6>
                    <div class="classes__item__text p-0 mt-2">
                        <div class="row">
                            <div class="col-4"> 
                                <a href="{{ path('tipo_edit', {'id': tipo.id}) }}" class="class-btn">Editar</a>
                            </div>
                            <div class="col-4">
                                {{ include('tipo/_delete_form.html.twig') }}
                            </div>
                        </div>
                        <a href="{{ path('tipo_index') }}" class="class-btn text-nowrap mt-2">Volver al listado</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {# <table class="table">
        <tbody>
            <tr>    
                <th>Id</th>
                <td>{{ tipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo.nombre }}</td>
            </tr>
            <tr>
                <th>Fecha creacion</th>
                <td>{{ tipo.fechacreacion ? tipo.fechacreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_index') }}">Volver al listado</a>

    <a href="{{ path('tipo_edit', {'id': tipo.id}) }}">Editar</a>

    {{ include('tipo/_delete_form.html.twig') }} #}
{% endblock %}
