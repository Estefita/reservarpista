{% extends 'base.html.twig' %}

{% block title %}Edit Tipo{% endblock %}

{% block body %}
<section class="breadcrumb-option set-bg" data-setbg="{{ asset('img/breadcrumb.jpg') }}">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb__text">
                        <h2>Contacto</h2>
                        <div class="breadcrumb__widget">
                            <a href="/home">Inicio</a>
                            <span>Contacto</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <h1>Edit Tipo</h1>

    {{ include('tipo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tipo_index') }}">back to list</a>

    {{ include('tipo/_delete_form.html.twig') }}
{% endblock %}
