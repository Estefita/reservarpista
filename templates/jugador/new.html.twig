{% extends 'base.html.twig' %}

{% block title %}New Jugador{% endblock %}

{% block body %}
	{% include "section/section.html.twig" with {'descripcion':'Jugador'} %}
	<div class="container-fluid p-5">
		<h3 class="text-center">Crear Nuevo Jugador</h3>
		<div class="row justify-content-center">
			<div class="col-4">
				{{ include('jugador/_form.html.twig') }}
				<div class="classes__item__text pl-0 text-center">
					<a href="{{ path('jugador_index') }}" class="class-btn">Volver al listado</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
