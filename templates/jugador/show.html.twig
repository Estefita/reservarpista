{% extends 'base.html.twig' %}

{% block title %}Jugador{% endblock %}

{% block body %}
{% include "section/section.html.twig" with {'descripcion':'Jugador'} %}
    <div class="container-fluid p-5">
    <h3 class="text-center">Jugador</h3>
    <div class="row justify-content-center">
    <div class="col-lg-4 col-md-6">
                <div class="classes__item classes__item__page">
                    <div class="classes__item__pic set-bg" data-setbg="img/classes/classes-3.jpg">
                        <span>Foto</span>
                    </div>                            
                    <h6>Nombre: {{ jugador.nombre }}</h6>
                    <h6>Apellidos: {{ jugador.apellidos }}</h6>
                    <h6>Dirección: {{ jugador.direccion }}</h6>
                    <h6>Provincia: {{ jugador.provincia }}</h6>
                    <h6>Población: {{ jugador.poblacion }}</h6>
                    <h6>Teléfono: {{ jugador.telefono }}</h6>
                    <h6>Fecha creación: {{ jugador.fechacreacion ? jugador.fechacreacion|date('Y-m-d H:i:s') : '' }}</h6>
                  
                    <div class="classes__item__text p-0 mt-2">
                        <div class="row">
                            <div class="col-4"> 
                                <a href="{{ path('jugador_edit', {'id': jugador.id}) }}" class="class-btn">Editar</a>
                            </div>
                            <div class="col-4">
                                {{ include('jugador/_delete_form.html.twig') }}
                            </div>
                        </div>
                        <a href="{{ path('jugador_index') }}" class="class-btn text-nowrap mt-2">Volver al listado</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jugador.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ jugador.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ jugador.apellidos }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ jugador.direccion }}</td>
            </tr>
            <tr>
                <th>Provincia</th>
                <td>{{ jugador.provincia }}</td>
            </tr>
            <tr>
                <th>Poblacion</th>
                <td>{{ jugador.poblacion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ jugador.telefono }}</td>
            </tr>
            <tr>
                <th>Fecha creacion</th>
                <td>{{ jugador.fechacreacion ? jugador.fechacreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table> #}
    {# {{ include('jugador/_delete_form.html.twig') }} #}
{% endblock %}
