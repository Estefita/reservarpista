{% extends 'base.html.twig' %}

{% block title %}Edit Deporte{% endblock %}

{% block body %}
	{% include "section/section.html.twig" with {'descripcion':'Deporte'} %}
	<div class="container-fluid p-5">
		<h3 class="text-center">Editar Deporte</h3>
		class="text-center">Editar Tipo</h3>
	<div class="row justify-content-center">
		<div class="col-4">
			<div class="classes__item__text">
				{{ include('deporte/_form.html.twig', {'button_label': 'Update'}) }}
				<a href="{{ path('deporte_index') }}" class="class-btn">Volver al listado</a>
				{# {{ include('deporte/_delete_form.html.twig') }} #}
			</div>
		</div>
	</div>
</div>{% endblock %}
