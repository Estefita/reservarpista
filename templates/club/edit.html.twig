{% extends 'base.html.twig' %}

{% block title %}Editar Club
{% endblock %}

{% block body %}
	{% include "section/section.html.twig" with {'descripcion':'Club'} %}
	<div class="container-fluid p-5">
		<h3 class="text-center">Editar Club</h3>
		<div class="row justify-content-center">
			<div class="col-4">
				<div class="classes__item__text">
					{{ include('club/_form.html.twig', {'button_label': 'Actualizar'}) }}

					<a href="{{ path('club_index') }}">Volver al listado</a>
				</div>
			</div>
		</div>
	</div>
	{# {{ include('club/_delete_form.html.twig') }} #}
{% endblock %}
